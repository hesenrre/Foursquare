<head>
  <meta name='viewport' content='width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no' />
  <script src="http://code.jquery.com/jquery-1.9.1.js"></script>
  <script src='http://api.tiles.mapbox.com/mapbox.js/v1.0.2/mapbox.js'></script>
  <link href='http://api.tiles.mapbox.com/mapbox.js/v1.0.2/mapbox.css' rel='stylesheet' />
  <%= javascript_include_tag :defaults %>
  <%= stylesheet_link_tag "application" %>
</head>
<body>
<div id="main">
  <div id = "content">
    <button type="button" onclick="centerMap()"><%= image_tag "locations.png" %></button>
    <br/>
    <br/>
    <br/>
    <br/>
    <input id="inputText" type="text">
    <button type="button" onclick="busqueda()">Búsqueda</button>
    <br/>

    <table border="0">
    <tr>
      <td>
        Item1
      </td>
      <td>
        <input id="quitItem1" type="button">
      </td>
    </tr>
    <tr>
      <td>
        Item2
      </td>
      <td>
        <input id="quitItem2" type="button">
      </td>
    </tr>
    <tr>
      <td>
        Item3
      </td>
      <td>
        <input id="quitItem3" type="button">
      </td>
    </tr>
    <tr>
      <td>
        Item4
      </td>
      <td>
        <input id="quitItem4" type="button">
      </td>
    </tr>
    <tr>
      <td>
        Item5
      </td>
      <td>
        <input id="quitItem5" type="button">
      </td>
    </tr>
    </table>

    
    

  </div>
  
  <div id='map'></div>
</div>
<script type='text/javascript'>
var lat = 0;
var lng = 0;
var map = null;
var arrayMarkers = null;
var contMarker = 0;
var putMarker = function(data)
{
  arrayMarkers = new Array();
  contMarker = 0;
  for(i = 0; i < data.length; i++)
  {
    auxCategories = data[i].categories;
    auxLocation = data[i].location;
    if(auxCategories[0] != undefined )
    {
      var latLng = new L.LatLng(
      auxLocation.lat,
      auxLocation.lng);

      var leafletIcon = L.icon({
        iconUrl: auxCategories[0].icon,
        iconSize: new L.Point(32,32),
        iconAnchor: new L.Point(16, 41),
        popupAnchor: new L.Point(0, -51),
        options: null
      });
      var marker = new L.Marker(latLng, {icon: leafletIcon})
        .bindPopup(data[i].name, { closeButton: false })
        .on('mouseover', function(e) { this.openPopup(); })
        .on('mouseout', function(e) { this.closePopup(); });
        map.addLayer(marker);
      arrayMarkers[contMarker] = marker;
      contMarker++;
    }
  }
};

var clearMarker = function()
{
  for(i = 0; i < contMarker; i++)
  {
    map.removeLayer(arrayMarkers[i]);
  }
  arrayMarkers = null;
};

var busqueda = function()
{
  var data = null;
  if($("#inputText").val() != "")
    data = {lat: lat, lng: lng, search: $("#inputText").val()};
  else
    data = {lat: lat, lng: lng};

  clearMarker();
  $.post("/load_venues",data).done(function(data){putMarker(data)});
};

var centerMap = function()
{
  map.setView(new L.LatLng(lat, lng), 15);

};
navigator.geolocation.getCurrentPosition(function(data) {
    lat = data['coords']['latitude'];
    lng = data['coords']['longitude'];
    /* Create map. */
    map = L.mapbox.map('map', 'examples.map-4l7djmvo')
    .setView(new L.LatLng(lat, lng), 15);
    var coords = {lat: lat, lng: lng};
    $.post("/load_venues",coords).done(function(data){putMarker(data)});
  });

</script>
</body>

